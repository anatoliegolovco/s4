# tests — validation & regression

See `docs/spec/sintez2-recon-spec.md` §5.4, §10 and milestones T7/T9.

- ZEXDOC/ZEXALL (CPU correctness via CRC) — the bar.
- FUSE test suite (per-instruction T-states & memory accesses).
- Patrik Rak contention/floating-bus tests: **N/A for Sintez 2 — must NOT pass**
  (no memory contention). Passing them = wrong machine.

(Skeleton — not yet implemented.)
